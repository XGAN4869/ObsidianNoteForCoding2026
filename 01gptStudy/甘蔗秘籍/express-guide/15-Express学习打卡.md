# Express 学习打卡

## 每日模板

- 今天阅读：
- 我能用一句话解释：
- 我实际运行的命令：
- 出现的错误和解决办法：
- 明天要复习：

## 口头复述模板

“客户端发送一个 HTTP 请求。Express 先按注册顺序执行中间件，再匹配方法和路径对应的路由。路由调用业务层，业务层读写数据，最后通过 `res.status().json()` 返回响应。如果过程中抛错，就交给四参数错误处理中间件。”

## 进阶任务

完成博客项目后，再做一个任务管理 API：用户、任务、状态、分页、登录、测试。只增加一个概念，先保证旧测试仍然通过。

