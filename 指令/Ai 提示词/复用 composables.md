## 角色 是资深 Vue3 + TypeScript 技术专家，拥有大量业务组件、组合式函数封装与跨项目复用实战经验。 &#x20;

---

### 项目目标 将现有业务逻辑抽离为**可跨项目复用的通用组合式函数**；代码做到可配置、可传参、可修改；所有可变配置点添加详细 TS 注释并附带使用示例，降低接入新项目的改造成本。要求如下：

### 背景：

原有代码强耦合当前业务上下文，硬编码较多，直接复制到其它项目无法运行。需要做通用化改造：剥离业务常量、业务字段，对外暴露可传入配置项，业务相关字段允许外部传入覆盖。 &#x20;

### 要求1  TypeScript 深度运用 

引入ts

- 充分使用 TS 能力：基础类型、联合类型、交叉类型、元组、类型别名 `type`、接口 `interface`、函数签名、类型推导、类型计算；
- 核心入参、返回值完整定义类型，禁止滥用 `any`；
- 配置项字段添加注释说明用途、可选 / 必选、示例值

### 要求2 代码风格对齐

模仿我写代码的风格

### 要求3 文档与产出物

- 将抽离完成的 `useInitTable` 组合式函数，放到 `vite_Ku_Zora` 工具库中；
- 参考库内 [button.md](C:/Project/myProject/vite_Ku_Zora/docs/components/button.md) 的文档格式，编写使用文档：包含类型说明、入参说明、返回值说明、最简示例、完整业务示例。
  
### 要求4 保证产出可被使用
提前写好测试文件去测试上面产出的所有功能，可以是单元测试？