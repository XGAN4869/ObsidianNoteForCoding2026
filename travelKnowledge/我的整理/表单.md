## 筛选组件

### 常见属性
- input
	- clearable -- 可清除
	- placeholder
	- value -- 配合下拉框，选中的 t-option 直接填入 value
	- key
	  
	- label：前端页面显示的名字
- table
	- :data="tableData" -- 数据源
	- :columns="columns" -- 表头，前端自定义
	- :pagination="pagination" @pageChange="onPageChange" 分页部分
	- :loading="loading" -- 加载态

## 1. 数据统一管理

这个文件把表单数据统一放进了 `formData`。

```
const createEmptyForm = () => ({  accountId: '',  accountName: '',  type: '',  amount: undefined,  reasonOption: '',  customReason: '',  remark: '',  date: '',})
```

然后用：

```
const formData = reactive(createEmptyForm())
```

这点很好。

因为这些字段都是表单相关数据：

```
目标员工 → accountId / accountName奖惩性质 → type关联金额 → amount奖惩事由 → reasonOption / customReason奖惩时间 → date备注 → remark
```

---

## 2. 模板绑定保持一致

你的模板基本保持了三者一致：

```
<t-form-item label="目标员工:" name="accountId">  <DriverSelect v-model="formData.accountId" /></t-form-item>
```

这里对应关系是：

```
name="accountId"v-model="formData.accountId"校验字段 accountId
```

其他字段也是同理：

```
type          → formData.typereasonOption  → formData.reasonOptioncustomReason  → formData.customReasonamount        → formData.amountdate          → formData.dateremark        → formData.remark
```

这就是标准表单写法。

---

## 3. 配置常量单独抽离

奖惩类型、奖惩事由、自定义值、日期配置都抽出来了。

```
const REWARD_TYPE = '奖励'const PUNISH_TYPE = '惩罚'const CUSTOM_REASON_VALUE = '__custom__'
```

奖惩类型选项：

```
const createRewardTypeOptions = [  { label: REWARD_TYPE, value: REWARD_TYPE },  { label: PUNISH_TYPE, value: PUNISH_TYPE },]
```

奖惩事由选项：

```
const rewardReasonOptions = [  { label: '迟到/早退', value: '迟到/早退', type: PUNISH_TYPE },  { label: '全勤奖', value: '全勤奖', type: REWARD_TYPE },  { label: '-- 自定义输入原因 --', value: CUSTOM_REASON_VALUE },]
```

这点也很好，模板不会被一堆死数据污染。

小建议：`createRewardTypeOptions` 这个名字可以改成：

```
const rewardTypeOptions = [...]
```

因为它不是函数，不需要叫 `createXxx`。

---

## 4. 派生数据使用 `computed`

你的代码里有好几个典型的派生数据。

### 根据奖惩性质过滤事由

```
const filteredRewardReasonOptions = computed(() => {  if (!formData.type) return rewardReasonOptions  return rewardReasonOptions.filter(    (item) => !item.type || item.type === formData.type  )})
```

意思是：

```
没选奖惩性质 → 显示全部原因选择奖励 → 只显示奖励原因 + 自定义选择惩罚 → 只显示惩罚原因 + 自定义
```

这是很标准的 `computed` 场景。

---

### 根据奖惩性质控制金额范围

```
const isRewardType = computed(() => formData.type === REWARD_TYPE)const isPunishType = computed(() => formData.type === PUNISH_TYPE)const amountMin = computed(() => isRewardType.value ? 0.01 : undefined)const amountMax = computed(() => isPunishType.value ? -0.01 : undefined)
```

意思是：

```
奖励 → 金额必须为正数惩罚 → 金额必须为负数
```

然后模板里直接绑定：

```
<t-input-number  v-model="formData.amount"  :min="amountMin"  :max="amountMax"/>
```

这个写法很好。

---

## 5. 字段联动使用 `watch`

这里用了 `watch` 处理字段联动。

```
watch(  () => formData.type,  () => {    const currentReasonIsValid = filteredRewardReasonOptions.value.some(      (item) => item.value === formData.reasonOption,    )    if (!currentReasonIsValid) {      formData.reasonOption = ''      formData.customReason = ''    }  },)
```

作用是：

```
当奖惩性质变化时检查当前奖惩事由是否合法如果不合法清空 reasonOption 和 customReason
```

例如：

```
原来选的是：奖励 → 全勤奖后来切换成：惩罚那么“全勤奖”就不该继续保留
```

这个就是典型的字段联动。

---

## 6. 校验规则集中管理

你现在有一部分规则写在模板里：

```
<t-form-item  name="accountId"  :rules="[{ required: true, message: '请选择目标员工', trigger: 'change' }]">
```

金额规则单独抽出来了：

```
const amountRules = computed(() => [  { required: true, message: '请输入关联金额', trigger: 'blur' },  { validator: (value) => Number(value) !== 0, message: '金额不能为0' },  { validator: validateAmountByRewardType, message: '奖励金额需 > 0，惩罚金额需 < 0' },])
```

金额规则抽得很好。

不过如果完全按照规范，可以把所有规则统一抽出来：

```
const rules = {  accountId: [{ required: true, message: '请选择目标员工', trigger: 'change' }],  type: [{ required: true, message: '请选择奖惩性质', trigger: 'change' }],  reasonOption: [{ required: true, message: '请选择奖惩事由', trigger: 'change' }],  date: [{ required: true, message: '请选择奖惩时间', trigger: 'change' }],}
```

然后模板里可以更干净。

---

## 7. 初始化与回显逻辑分离

这一块你写得比较标准。

### 初始化表单

```
const resetFormData = () => {  Object.assign(formData, createEmptyForm())}
```

### 编辑回显

```
const fillFormData = (detail = {}) => {  const reason = detail.reason || ''  Object.assign(formData, {    accountId: detail.accountId || '',    accountName: detail.accountName || '',    type: detail.type || '',    amount: detail.amount ? Number(detail.amount) : undefined,    reasonOption: isPresetReason(reason) ? reason : CUSTOM_REASON_VALUE,    customReason: isPresetReason(reason) ? '' : reason,    remark: detail.remark || '',  })}
```

逻辑是：

```
新增 → resetFormData()编辑 → fillFormData(editData)关闭 → resetFormData()
```

这一点很好。

不过这里有一个小问题：你 `formData` 里面有 `date`，提交时也用了 `rewardPenaltyTime`，但是 `fillFormData` 里面没有回显日期。

建议补上：

```
date: detail.rewardPenaltyTime || detail.date || '',
```

否则编辑时奖惩时间可能不会回显。

---

## 8. 提交流程固定规范

你的提交流程是标准的：

```
const handleSubmit = async ({ close }) => {  const valid = await formRef.value?.validate?.()  if (valid !== true) return  submitLoading.value = true  try {    await apiCreateRewardApplication(buildPayload)    MessagePlugin.success('新增奖惩记录成功')    close()    resetFormData()    emit('success')  } finally {    submitLoading.value = false  }}
```

实际顺序是：

```
防止重复提交表单校验格式化金额组装接口参数调用新增接口成功提示关闭弹窗重置表单通知父组件刷新
```

这是非常好的固定流程。

不过可以进一步把参数组装抽出来：

```
const buildSubmitPayload = () => ({  accountId: Number(formData.accountId),  accountName: formData.accountName,  type: formData.type,  amount: formatAmount(formData.amount),  reason: formData.reasonOption === CUSTOM_REASON_VALUE    ? formData.customReason.trim()    : formData.reasonOption,  remark: formData.remark?.trim() || '',  rewardPenaltyTime: formData.date || '',})
```

然后提交里只写：

```
await apiCreateRewardApplication(buildSubmitPayload())
```

这样提交函数会更清楚。

---


```
这个组件是一个奖惩记录弹窗表单。

数据层：
formData 统一管理 accountId、type、amount、reason、date、remark。

配置层：
奖惩类型、奖惩事由、自定义原因值、日期配置都抽成常量。

派生层：
filteredRewardReasonOptions 根据 type 过滤原因；
amountMin / amountMax 根据奖励或惩罚限制金额。

联动层：
监听 type 变化，如果当前 reason 不合法，就清空原因。

初始化层：
resetFormData 负责新增；
fillFormData 负责编辑回显。

提交层：
先 validate，再组装 payload，再调 apiCreateRewardApplication，
成功后提示、关闭弹窗、重置表单、通知父组件刷新。
```

## form 与 t-form
### 1. 先定义表单数据 `formData`

> 只要是表单项，就优先放进 `formData`。

表单里所有需要填写、校验、提交的字段，都统一放进 `formData`。

```js
const createEmptyForm = () => ({
  accountId: '',
  accountName: '',
  type: '',
  amount: undefined,
  reasonOption: '',
  customReason: '',
  remark: '',
  date: '',
})

const formData = reactive(createEmptyForm())
```

例如：

```txt
目标员工 → formData.accountId
奖惩性质 → formData.type
奖惩事由 → formData.reasonOption
金额 → formData.amount
日期 → formData.date
备注 → formData.remark
```

---

### 2. 模板中保持三者一致

每一个表单项都要注意这三个地方对应：

```vue
<t-form :data="formData">
```

```vue
<t-form-item name="date">
```

```vue
<t-date-picker v-model="formData.date" />
```

也就是：

```txt
:data="formData"
name="date"
v-model="formData.date"
```

这三个要对齐，否则表单校验会找不到字段。

---

###  3. 配置类数据单独抽出来

比如下拉选项、常量值、日期配置，不要直接写死在模板里。

```js
const REWARD_TYPE = '奖励'
const PUNISH_TYPE = '惩罚'

const createRewardTypeOptions = [
  { label: REWARD_TYPE, value: REWARD_TYPE },
  { label: PUNISH_TYPE, value: PUNISH_TYPE },
]

const defaultConfig = {
  placeholder: '请选择日期',
  format: 'YYYY-MM-DD',
  valueType: 'YYYY-MM-DD',
}
```

这样模板更干净，后期也更好改。

---

###  4. 有依赖关系的内容用 `computed`

比如奖惩事由要根据奖惩性质变化：

```js
const filteredRewardReasonOptions = computed(() => {
  if (!formData.type) {
    return rewardReasonOptions
  }

  return rewardReasonOptions.filter((item) => {
    return !item.type || item.type === formData.type
  })
})
```

金额的最大值 / 最小值也属于派生状态：

```js
const isRewardType = computed(() => formData.type === REWARD_TYPE)
const isPunishType = computed(() => formData.type === PUNISH_TYPE)

const amountMin = computed(() => (isRewardType.value ? 0.01 : undefined))
const amountMax = computed(() => (isPunishType.value ? -0.01 : undefined))
```

简单说：

```txt
由 formData 推导出来的值 → 用 computed
```

---

###  5. 字段之间有联动，用 `watch`

比如切换了“奖励 / 惩罚”，原来的事由可能不合法了，所以要清空：

```js
watch(
  () => formData.type,
  () => {
    const currentReasonIsValid = filteredRewardReasonOptions.value.some(
      (item) => item.value === formData.reasonOption,
    )

    if (!currentReasonIsValid) {
      formData.reasonOption = ''
      formData.customReason = ''
    }
  },
)
```

记住：

```txt
一个字段变化，需要影响另一个字段 → 用 watch
```

---

###  6. 校验规则单独管理

简单规则可以直接写在模板里：

```vue
:rules="[{ required: true, message: '请选择奖惩时间', trigger: 'change' }]"
```

复杂规则建议抽成变量：

```js
const amountRules = computed(() => [
  { required: true, message: '请输入关联金额', trigger: 'blur' },
  { validator: (value) => Number(value) !== 0, message: '金额不能为0', trigger: 'blur' },
  { validator: validateAmountByRewardType, message: '奖励金额需 > 0，惩罚金额需 < 0', trigger: 'blur' },
])
```

复杂校验再抽函数：

```js
const validateAmountByRewardType = (value) => {
  const amount = Number(value)

  if (!Number.isFinite(amount)) {
    return false
  }

  if (isRewardType.value) {
    return amount > 0
  }

  if (isPunishType.value) {
    return amount < 0
  }

  return true
}
```

这里建议你把原来的：

```js
amount >= 0
amount <= 0
```

改成：

```js
amount > 0
amount < 0
```

因为你前面已经写了“金额不能为 0”。

---

###  7. 打开弹窗时填充，关闭弹窗时重置

新增时重置：

```js
const resetFormData = () => {
  Object.assign(formData, createEmptyForm())
}
```

编辑时回填：

```js
const fillFormData = (detail = {}) => {
  const reason = detail.reason || ''

  Object.assign(formData, {
    accountId: detail.accountId || '',
    accountName: detail.accountName || '',
    type: detail.type || '',
    amount: detail.amount ? Number(detail.amount) : undefined,
    reasonOption: isPresetReason(reason) ? reason : reason ? CUSTOM_REASON_VALUE : '',
    customReason: isPresetReason(reason) ? '' : reason,
    remark: detail.remark || '',
    date: detail.create_Time || detail.date || '',
  })
}
```

你现在的 `fillFormData` 里少了 `date`，如果以后编辑弹窗也要回显日期，记得补上。

---

###  8. 提交时统一处理：校验 → 组装参数 → 调接口

提交函数一般固定这样写：

```js
const handleSubmit = async ({ close }) => {
  if (submitLoading.value) return

  const valid = await formRef.value?.validate?.()
  if (valid !== true) return

  submitLoading.value = true

  try {
    const amount = formatAmount(formData.amount)

    await apiCreateRewardApplication({
      accountId: Number(formData.accountId),
      accountName: formData.accountName,
      type: formData.type,
      amount,
      reason:
        formData.reasonOption === CUSTOM_REASON_VALUE
          ? formData.customReason.trim()
          : formData.reasonOption,
      remark: formData.remark?.trim() || '',
      create_Time: formData.date || '',
    })

    MessagePlugin.success('新增奖惩记录成功')
    close()
    resetFormData()
    emit('success')
  } catch {
    MessagePlugin.error('新增奖惩记录失败')
  } finally {
    submitLoading.value = false
  }
}
```

提交逻辑的顺序记住：

```txt
防重复提交
→ 表单校验
→ loading = true
→ 整理接口参数
→ 调接口
→ 成功提示
→ 关闭弹窗
→ 重置表单
→ 通知父页面刷新
→ loading = false
```

---

###  以后写 Form 的固定顺序

你可以按这个顺序写，基本不会乱：

```txt
1. 定义 visible / props / emit
2. 定义 formRef
3. 定义 createEmptyForm
4. 定义 formData
5. 定义下拉选项 / 常量
6. 定义 computed 派生数据
7. 定义 watch 联动逻辑
8. 定义 rules 校验规则
9. 定义 resetFormData / fillFormData
10. 定义 dialogButtons
11. 定义 handleSubmit
12. 写 template
```

---

###  一句话总结

这种表单代码的核心思路是：

> **formData 管数据，rules 管校验，computed 管派生，watch 管联动，handleSubmit 管提交，reset/fill 管弹窗状态。**

你以后看到复杂表单，不要一上来盯着模板看。先看这几块：  
**数据在哪、校验在哪、联动在哪、提交时发了什么参数。**