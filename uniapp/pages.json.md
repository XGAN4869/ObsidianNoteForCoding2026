- `pages` 数组（路由表 + 全局页面配置）
	- 是页面注册表/路由,每个对象都表示一个页面路由
	- `style` 是页面窗口配置，
- `easycom` 精简 安装+引入+注册 流程，配好了直接写 组件名即可
- `globalStyle` 全局窗口样式配置
	- uniapp 启动后会先读取pages
- `uniIdRouter`  用户身份路由配置（用于权限控制）
- `tabBar` 底部选项卡配置