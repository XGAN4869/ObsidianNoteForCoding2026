# 基础中的基础

## 分类索引

### 响应式与状态管理

- [[基础中的基础#reactive / ref|reactive / ref]]
- [[基础中的基础#computed / watch|computed / watch]]
- [[基础中的基础#store 和 localStorage|store 和 localStorage]]
- [[基础中的基础#Pinia|Pinia 基础]]
- [[基础中的基础#Pinia 跨 Store 状态使用|Pinia 跨 Store 使用]]
- [[基础中的基础#const / let 与响应式对象|const / let 与响应式对象]]
- [[基础中的基础#composables 组合式函数(放局部状态，如果是全局状态)|composables 组合式函数]]
- [[基础中的基础#持久化 pinia-plugin-persistedstate|Pinia 持久化]]

### Vue 组件与模板

- [[基础中的基础#冒泡事件|事件冒泡]]
- [[基础中的基础#defineModel|defineModel 基础]]
- [[基础中的基础#:class 动态 class 的写法是 { ... }|动态 class]]
- [[基础中的基础#css 样式覆盖 Cascade|CSS 样式覆盖]]
- [[基础中的基础#useSlots / useSlot|Slots]]
- [[基础中的基础#Schema-Driven|Schema-Driven]]
- [[基础中的基础#v-show / v-if|v-show / v-if]]
- [[基础中的基础#v-for 搭配 :key 适配 diff 算法|v-for 与 key]]
- [[基础中的基础#defineModel 编译宏，|多 v-model]]
- [[基础中的基础#定时器|定时器与组件销毁]]
- [[基础中的基础#Vue 中的 key 给每个 VNode 节点唯一的 id, diff 优化|Vue key 与 diff]]
- [[基础中的基础#nextTick 等本轮 DOM 全部渲染完再执行xxx逻辑|nextTick]]

### JavaScript 与异步

- [[基础中的基础#Promise 基础用法|Promise]]
- [[基础中的基础#Set 和 Array|Set 与 Array]]
- [[基础中的基础#[]数组解构 / {}对象解构 浅拷贝|解构与浅拷贝]]
- [[基础中的基础#await/async|async / await]]
- [[基础中的基础#扩展运算符 ...  / 剩余参数 Rest|扩展与剩余参数]]
- [[基础中的基础#函数不能用 fn?()，函数@clear="() => item.onClear?.()"|可选调用]]
- [[基础中的基础#Object.assign 批量赋值|Object.assign]]
- [[基础中的基础#变量做属性名 [变量]|变量属性名]]
- [[基础中的基础#宏任务 / 微任务与这一区别的关系|宏任务与微任务]]

### 路由、架构与工程化

- [[基础中的基础#router|router]]
- [[基础中的基础#工厂模式 or 单例模式|工厂模式与单例模式]]
- [[基础中的基础#手搭Monorepo|Monorepo]]

## 具体知识点

### reactive / ref
数组 ref, 对象一般 reactive
why reactive 不能 [], 因为**不能直接整体重新赋值**

| 特性       | `ref`                                                | `reactive`                                                                   |
| :------- | :--------------------------------------------------- | :--------------------------------------------------------------------------- |
| **适用类型** | 原始值 (number, string, boolean) 和 对象                   | **仅对象** (包括数组、Map、Set 等复杂类型) <br>why？因为底层return new Proxy...，Proxy 只能代理上面的内容 |
| **访问方式** | `.value` 访问/修改值                                      | 直接访问/修改属性                                                                    |
| **底层实现** | 包装成一个带有 `.value` 属性的**对象**，用 `Proxy` 拦截 `.value` 的读写 | 直接用 `Proxy` 包装目标对象本身                                                         |
| **解构问题** | 解构会失去响应性                                             | 解构会失去响应性 (`...` 扩展运算符也会)                                                     |
| **内部转换** | `reactive` 内部如果遇到 `ref`，会自动解包 (unwrap)               |                                                                              |
- reactive/ref的目的是让被定义变量本身活起来，而不是监听变量内部属性是否有变化来变化。比如 const profile = reactive({ name: accountStore.accountName })，它只在初始化时读取一次 store。解构失去响应性
	```bash
	reactive 的响应性来自 Proxy。
	
	直接访问 state.name：
	会走 Proxy get，所以有响应性。
	
	解构 const { name } = state：
	只是在那一刻复制了 state.name 的值。
	
	后面访问 name：
	访问的是普通变量，不再走 Proxy get。
	
	所以：
	不会 track，不会收集依赖，也就不会响应式更新。
	```
![[Pasted image 20260615181924.png|357]]
### computed / watch
- computed 
	- 注意事项：只能存在组件里。它是组件专属API，只依附 setup 组件环境，脱离组件就用不了
	- 作用：依托组件内变量，算出新值，给模板渲染用，只负责取值，不负责做事
	- 底层详解
		- 
2. watch 不绑定组件
[响应式 API：核心 | Vue.js](https://cn.vuejs.org/api/reactivity-core#watch)
只要拿到响应式变量，随便在哪都能写
自定义指令、工具函数、独立JS文件、仓库里全都能用
作用：变量一变，立刻执行一段逻辑、操作DOM、发请求、改样式
3. 对应你权限场景
 
- 权限指令是独立DOM逻辑，不属于某个组件，写不了computed
- 只需要监听Pinia权限数组，数组一改就删按钮/显示按钮
- 这种脱离组件、监听变量执行动作，只能用watch
 
4. 最简区别
 
- computed：组件内算值，给页面显示
- watch：任意地方监听值变化，执行行为
	- ```js
	  //这里不能写 getters 函数式，ref本身不变的,改的是 ref 里面的 value 
		watch(activePath, () => {  
		  console.log('activePath', activePath.value)  
		},{immediate:true})
		
	//或者改成这种
	//这里不能写 getters 函数式，ref本身不变的  
		watch(  
		  () => activePath,  
		  () => {  
		    console.log('activePath', activePath.value)  
		  },  
		  { immediate: true, deep: true },  
		)
	  ```
- computed + v-model 使用场景
	`computed.get` 负责把数组里的 checked 状态转换成 radio-group 能识别的选中 id；`computed.set` 负责把 radio-group 新选中的 id 写回数组里的 checked 状态。
	```js
	
	```

### 冒泡事件
- 这个 permissionView 如果放在里面，外面是 v-for 然后配合 tap，那就算tap隐藏了，依旧会加在隐藏的元素快上
```vue
<view class="action-grid" >
		<PermissionView v-for="item in filteredActionItems"
						:key="item.title"
						:code="item.permissionValue" >
		  <view class="action-item"
				:class="item.type"
				@tap="handleActionTap(item.title)" >
			<image class="action-icon" :src="item.icon" mode="aspectFit" />
			<text class="action-title" >{{ item.title }}</text >
		  </view >
		</PermissionView >
	  </view >
```
### Promise 基础用法
```js
// 返回Promise的登录接口
function loginApi() {
  return new Promise((resolve, reject) => {
    setTimeout(() => {
      const ok = false;
      if (ok) resolve({ token: "xxx" });
      else reject(new Error("账号错误"));
    }, 1000);
  });
}

// 链式调用
loginApi()
  .then(res => console.log("登录成功", res))
  .catch(err => console.log("失败", err))
```

### store 和 localStorage

- 一般项目的做法
	登录 token：uni storage 持久化
	登录用户信息：Pinia store + persist 持久化
	启动后用户信息：App.vue 又调用 getSystemInfo(true) 重新刷新
	页面使用：各页面直接读 accountStore
- pc端菜单栏
	
	浏览器刷新
	↓
	JS 重新执行，Pinia 重新创建
	↓
	pinia-plugin-persistedstate 从 localStorage 恢复 token 等字段
	↓
	进入 router.beforeEach
	↓
	发现 accountStore.token 还在
	↓
	调用 accountStore.getLoginInfo()
	↓
	重新请求后端登录信息
	↓
	permissionStore.loaded 是 false
	↓
	重新请求权限菜单
	↓
	动态路由重新添加

### defineModel
你的疑问很关键，这里确实容易搞混。先直接说答案：

```js
const startDate = defineModel('startDate', { default: '' });
// 这一行是完全正确的，且等同于 emit('update:startDate', innerStart.value)
startDate.value = innerStart.value;
```

**用 `defineModel` 时，直接赋值就是合法的，它就是触发 `emit` 的语法糖。**
`defineModel` 是 Vue 3.4+ 引入的编译宏。它在编译后会生成：

1. 一个 **props** 叫 `startDate`
    
2. 一个 **emit** 事件叫 `update:startDate`
    
3. 一个 **ref**（就是你拿到的 `startDate`）

### Set 和 Array
- 用 Set 中的 Set.has() 比 Array.includes() 查询更快一点

### []数组解构 / {}对象解构 浅拷贝
- const [resAccountId, resToken] = res.split('.'); -- split 后变数组
- 

### Pinia
[定义 Store | Pinia](https://pinia.vuejs.org/zh/core-concepts/)
- Option store。
- 在 _Setup Store_ 中：
	- `ref()` 就是 `state` 属性
	- `computed()` 就是 `getters`
	- `function()` 就是 `actions`
- pinia-plugin-persistedstate 持久化插件配置 -> [开始 | Pinia Plugin Persistedstate](https://prazdevs.github.io/pinia-plugin-persistedstate/zh/guide/) 

### 手搭Monorepo
[手把手带你玩转 Monorepo，拥抱现代前端开发新范式 | Grtsinry43 的前端札记](https://blog.grtsinry43.com/posts/monorepo-quick-guide/#chu-shi-hua-monorepo)

### 工厂模式 or 单例模式
- 看执行时机，是顶层就执行并 export 还是 函数中执行，然后export 函数
```js
//单例store
export const store = createPinia()

store.use(
  createPersistedState({
   ...
    }}))
export function initStore(app: App<Element>): void {
//注册store，但是store还是单例模式
  app.use(store)
}
```

### await/async
- 需要等待该 async 函数完成的时候，用 `await`  当作暂停器，比如某个数据必须先拿到。
```js
async function login(remember, options) {
  const res = await loginApi(...)
  // ...
  await getSystemInfo()   // 没 await，这里异步跑了，但不管它
  // 立即继续执行后面的 remember 逻辑
  // login 函数返回了
}
```

### 扩展运算符 ...  / 剩余参数 Rest
- 扩展运算符：数组 拆出 单值 --> 
- 剩余参数：合并成数组，后面的覆盖前面的
E.G 记住密码
```js
const formData = reactive({  
    user: '',  
    pass: '',  
    remember: shouldRememberLogin,  
    ...rememberedLoginParams  
});
```

### const / let 与响应式对象
- 不要写 let，会影响响应式变量，不能不小心给赋值了  
```js
const token = ref(getToken())
```

### :class 动态 class 的写法是 { ... }
```js
:class="{ 'ml-2': item.path !== '/home' }"
```

### router
- useRouter内部底层依赖inject，普通JS文件顶层执行直接报警！
```js
// 全局统一处理token过期  
if (response.data.code  === 401) {  
  //清除 token  
  clearToken()  
  //TODO: 之后故意写错看看会不会跳转,其实并不知道为什么这里不能在顶部 import useRouter 从 'vue-router' 中  
  //TODO: 只能引入本地注册过的 router  router.push('/login')  
}
```

### composables 组合式函数(放局部状态，如果是全局状态)

- 不同于 mixin，composables是**有状态逻辑**：不只是单纯工具函数，函数内部会保存数据（状态）
	- mixin如果不包工厂函数：所有组件在传参的状态下共用 mixin 内部同一个引用类型数据，造成状态污染。( 提示！mixin 本身的结构也是状态独立的，和 composables 一样有独立的 ref/reactive... )
- 不同于工具函数 utils，无状态，输入→输出
- 组合式函数：**自带状态、可以监听、可以绑定生命周期**。（两种方式👇)
	- 1. 如果你把 ref/reactive/computed/watch/onMounted 状态写在函数中 --> 就能隔离状态，每次调用独立。✅
	- 2. 状态提升到函数外部 → 全局单例，调用全部共享（❌不推荐，类似简易版 pinia）

### 持久化 pinia-plugin-persistedstate
- pinia-plugin-persistedstate 默认只序列化仓库内部 `ref()` / `reactive()` 包裹的响应式数据；普通 `const` 常量、函数、computed 默认不会存入本地存储。

### Pinia 跨 Store 状态使用
- 第一个参数是你的应用中 Store 的唯一 ID，Pinia 将用它来连接 store 和 devtools。 export const useAlertsStore = defineStore('alerts', { // 其他配置... }) https://pinia.vuejs.org/zh/core-concepts/
- 顶层调用
	- pinia 之间的状态是可以互相使用的，但是不能放在顶层
		```js
					// ✅ 组件 setup
					//<script setup>` 的代码等价于组件的setup() 函数，组件实例创建之
					//后才执行，此时 Pinia 已经被app.use(pinia)挂载完毕，组件上下文存
					//在，这里直接调用 `useXxxStore()` 是官方标准正确写法
					<script setup>
					const store = useSearchFilters()
					</script>
					
					// ✅ composable函数内部
					export function useFilterLogic() {
					  const store = useSearchFilters()
					  return { store }
					}
		```
### css 样式覆盖 Cascade

### useSlots / useSlot
```js
// 如果你写了
/*
<Component>
  <template #default>基础项</template>
  <template #show>高级项</template>
</Component>
*/
slots = {
  default: ()=>{},
  show: ()=>{}
}
Object.keys(slots) → ['default','show']
slotCount = 2
```

### Schema-Driven
利用了 vue3 的 v-bind 特性
```js
//取代标签，用配置 vue3很不错的机制  
const searchConfig = [  
  { field: 'username', label: '登录账号', type: 'input', placeholder: '请输入登录账号' },  
]
```

### v-show / v-if
|方式|DOM|表单数据|切换性能|适用展开收起？|
|---|---|---|---|---|
|v-show|永久存在|保留输入内容|切换快，首次渲染略耗|✅ 推荐|
|v-if|条件 false 直接移除|内容丢失|切换会重建，容易闪烁|❌ 不推荐|

### v-for 搭配 :key 适配 diff 算法

### defineModel 编译宏，
only setup 它用来**接收**父组件通过 `v-model` 传下来的数据
如果你在一个组件里需要**多个 v-model**（比如弹窗的 `visible` 和表单的 `formData`），就可以用具名 v-model：
父组件
```js
<child-dialog
  v-model:visible="dialogVisible"
  v-model:formData="myForm"
/>
```
<child-dialog
  v-model:visible="dialogVisible"
  v-model:formData="myForm"
/>

子组件：
```vue
<script setup>
const visible = defineModel('visible', { type: Boolean, default: false })
const formData = defineModel('formData', { type: Object, required: true })
</script>


<t-input v-model="modelValue[item.field]" />
```
上面的 formData 是一个ref，其 formData.value 即 父组件传过来的 myForm 对象
**模板中直接绑定，会自动更新 modelValue.value.username**

### 函数不能用 fn?()，函数@clear="() => item.onClear?.()"
### Object.assign 批量赋值
```js
// 直接批量赋值  
Object.assign(formData, {  
  username: initFormData.value.username,  
  nickname: initFormData.value.nickname,  
  mrDepartmentId: initFormData.value.mrDepartmentId,  
  status: String(initFormData.value.status),  
})

Object.assign(formData,createEmptyForm())

```

### 变量做属性名 [变量]
```js
formData[opt.key] = item?.option?.[opt.key] || ''
```

### 定时器
- Vue 组件里只要写了定时器，**一定要思考组件销毁要不要清掉**，KeepAlive 场景更容易踩内存泄漏
> [!HINT]
> 因为KeepAlive 的组件只是路由切走，**组件实例不会销毁！不会执行 onBeforeUnmount /onUnmounted**] 下次切回来：`onActivated` 激活，继续复用原来那个组件实例。

### Vue 中的 key 给每个 VNode 节点唯一的 id, diff 优化
[vue中key的原理详解（超详细，收藏好）_vue :key-CSDN博客](https://blog.csdn.net/weixin_69422396/article/details/135268485)

>[!HINT]
>`key`是给每一个`vnode`的唯一`id`，也是`diff`的一种优化策略，可以根据`key`，更准确， 更快的找到对应的`vnode`节点

### nextTick 等本轮 DOM 全部渲染完再执行xxx逻辑

常见场景🏠：滚动

### 宏任务 / 微任务与这一区别的关系

|概念|典型代表|任务存储位置|是否阻止 GC|
|---|---|---|---|
|宏任务|`setTimeout` `setInterval` I/O|浏览器定时器线程 / 事件队列|✅ 会阻止，直到任务被执行或取消|
|微任务|`Promise.then` `MutationObserver`|当前宏任务结束后立即执行的微任务队列|仅任务执行期间短暂持有，执行完即释放|
|普通对象|`AbortController` `{}` `[]`|无（仅受 JS 引用链管理）|❌ 不阻止，引用为 0 即回收|

### axios 拦截器
- axios.js 的错误拦截问题 -- 就是其实还是要向下抛出错误的，不然下层的错误就接不到
